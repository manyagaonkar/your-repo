Spring Boot Webhook Automation

This repository contains a Spring Boot application built for an automation workflow:
1. Generates a webhook and access token.
2. Solves a SQL task and submits the final query automatically.
